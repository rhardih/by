<div class="header">
	<h1>Trigger Build</h1>
	<h2>Standalone Toolchain for Android NDK in Docker</h2>
</div>

<div class="content">
  <p>Start a build on Travis for the following configuration.</p>

  <% unless locals[:flash].nil? %>
    <%= render_flash(flash) %>
  <% end %>

	<form class="pure-form pure-form-stacked" action="" method="post">
		<fieldset>
			<legend>Parameters</legend>

      <div class="pure-g">

        <div class="pure-u-1 pure-u-md-1-2">
          <label for="ndk">NDK</label>
          <input class="pure-input-1" id="ndk" type="text" value="<%= params[:ndk] %>" readonly>


          <label for="platform">Platform</label>
          <input class="pure-input-1" id="platform" type="text" value="<%= params[:platform] %>" readonly>


          <label for="toolchain">Toolchain</label>
          <input class="pure-input-1" id="toolchain" type="text" value="<%= params[:toolchain] %>" readonly>

        </div>
      </div>

      <button type="submit" class="pure-button pure-button-primary"
        <%= "disabled" if travis[:busy] %>>Submit</button>
    </fieldset>
  </form>
</div>
